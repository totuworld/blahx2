import { NextPage } from 'next';

/**
 * 각 사용자 개별 페이지
 *
 * 대답이 필요한 질문 목록
 * 대답된 질문 복록이 보인다.
 *
 * 다른 질문 보기 버튼으로 다시 목록으로 돌아갈 수 있음.
 */
const UserMyPage: NextPage = function () {
  return <>Hello world</>;
};

export default UserMyPage;
