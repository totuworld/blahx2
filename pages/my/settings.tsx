import { NextPage } from 'next';

/**
 * 사용자 셋팅 페이지
 *
 * 닉네임 설정
 * id 설정
 * 프로필 문구 설정
 *
 * twitter 계정 연결 버튼
 * (트위터로 가입된 경우는 계정 취소 버튼 노출, 단 연결 계정이 트위터 뿐이라면 불가능)
 */
const UserSettingsPage: NextPage = function () {
  return <>Hello world</>;
};

export default UserSettingsPage;
